classdef E5Parameters
    %E5PARAMETERS
    properties
        A %MATLAB ONLY: (1,1) {mustBeNumeric, mustBeReal, mustBeNonnegative}
        
        B %MATLAB ONLY: (1,1) {mustBeNumeric, mustBeReal, mustBeNonnegative}
        
        C %MATLAB ONLY: (1,1) {mustBeNumeric, mustBeReal, mustBeNonnegative}
        
        M %MATLAB ONLY: (1,1) {mustBeNumeric, mustBeReal, mustBeNonnegative}
    end
end
